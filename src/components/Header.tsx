import React from 'react';

export const Header = () => (
  <header className="mb-6">
    <h1 className="text-2xl font-bold">Metric Normalization Explorer</h1>
    <p className="text-sm text-gray-600 mt-1">
      Understand how raw telemetry is transformed into modeling-ready metrics across teams.
    </p>
  </header>
);
